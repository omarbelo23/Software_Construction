# TDD Evidence (Integration)

For each user story:
1) We first wrote the failing test (red).
2) We implemented/fixed the backend route/middleware (green).
3) We refactored shared helpers (db + auth helpers) while keeping tests passing (refactor).

Evidence:
- Tests use consistent US-IDs (US-AUTH-1, US-MENU-4, etc.) in descriptions.
- Git commit history shows tests added before/fixing route logic (recommended).
